import styled from 'styled-components';

export const FilledButtonContainer = styled.div`
  
`;

export const Button = styled.button`
    border: none;
    background: none;
    display: flex;
    align-items: center;
    justify-content: center;
    color: inherit;
`;
